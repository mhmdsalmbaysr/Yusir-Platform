/* ===================================================================
   إعدادات منصة يُسر
   لتفعيل المراجعات المشتركة بين جميع الزوار، أنشئ مشروعاً مجانياً على
   Supabase (https://supabase.com) وضع القيم أدناه. اترك الحقول فارغة
   لاستخدام التخزين المحلي (localStorage) كحل احتياطي.

   خطوات سريعة:
   1) أنشئ مشروعاً في Supabase.
   2) من Project Settings → API انسخ:  Project URL  و  anon public key.
   3) في SQL Editor شغّل المخطط الموجود في README (جدول reviews + سياسات RLS).
   4) ضع القيمتين هنا.

   ملاحظة أمان: مفتاح anon عام بطبيعته وآمن للنشر، وتحميه سياسات RLS.
=================================================================== */
window.YUSIR_CONFIG = {
    SUPABASE_URL: "",       // مثال: https://abcdxyz.supabase.co
    SUPABASE_ANON_KEY: "",  // مفتاح anon public

    // بيانات دخول السوبر أدمن (غيّرها فوراً)
    SUPER_ADMIN: { user: "superadmin", pass: "yusir@2024" }
};
